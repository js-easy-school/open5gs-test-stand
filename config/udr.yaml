# UDR — хранилище данных абонентов, работает поверх MongoDB.
logger:
  level: info

global:
  max:
    ue: 128

db_uri: mongodb://mongo/open5gs

udr:
  sbi:
    server:
      - address: 0.0.0.0
        port: 7777
    client:
      scp:
        - uri: http://scp:7777
